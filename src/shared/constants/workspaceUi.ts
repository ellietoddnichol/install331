/** Persisted room / takeoff view / selection; URL ?view=quantities for quantities mode. */
export const TAKEOFF_ALL_ROOMS = '__all__';

/** Pricing view: show all lines in the active room (no category filter). */
export const PRICING_ALL_CATEGORIES = '__all_categories__';
