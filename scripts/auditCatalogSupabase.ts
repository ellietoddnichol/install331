/**
 * Supabase / Postgres catalog audit entrypoint.
 * Same implementation as `npm run catalog:audit` — use with `DB_DRIVER=pg` and `DATABASE_URL=...`.
 *
 *   DB_DRIVER=pg DATABASE_URL=... npm run catalog:audit:supabase
 */
import './catalog-audit.ts';
